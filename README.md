macos menu bar app that allows you to long press space (without blocking shortpress) to quick switch between slack/iterm2, if neither is focused it jumps to slack, if slack isn't open iterm2, if neither is open, noop.

it has right click options to toggle start at startup (true by default) and exit application.
